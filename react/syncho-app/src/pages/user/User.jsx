// 1. 필요한 모듈 및 패키지 임포트
import React from 'react';
import './User.css'; // 컴포넌트 관련 스타일 파일

// 2. 함수형 컴포넌트 정의
function User() {
    // 3. 컴포넌트 내부 로직 및 상태 관리 (필요한 경우)

    return (
        // 4. JSX 반환 (렌더링할 UI)
        <div className="component-name">
            <h1>Hello, React!</h1>
            <p>This is Payment.</p>
        </div>
    );
}

// 5. 컴포넌트 내보내기
export default User;
